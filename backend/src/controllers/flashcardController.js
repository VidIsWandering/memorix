// eslint-disable-next-line no-unused-vars
import Flashcard from '../models/Flashcard.js';

const createFlashcard = async (_req, res) => {
  // eslint-disable-next-line no-unused-vars
  return res.status(501).json({ error: 'Not implemented' });
};

const getFlashcards = async (_req, res) => {
  // eslint-disable-next-line no-unused-vars
  return res.status(501).json({ error: 'Not implemented' });
};

const getFlashcard = async (_req, res) => {
  // eslint-disable-next-line no-unused-vars
  return res.status(501).json({ error: 'Not implemented' });
};

const updateFlashcard = async (_req, res) => {
  // eslint-disable-next-line no-unused-vars
  return res.status(501).json({ error: 'Not implemented' });
};

const deleteFlashcard = async (_req, res) => {
  // eslint-disable-next-line no-unused-vars
  return res.status(501).json({ error: 'Not implemented' });
};

export default {
  createFlashcard,
  getFlashcards,
  getFlashcard,
  updateFlashcard,
  deleteFlashcard,
};
