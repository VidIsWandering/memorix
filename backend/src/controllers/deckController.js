// eslint-disable-next-line no-unused-vars
import Deck from '../models/Deck.js';

const createDeck = async (_req, res) => {
  // eslint-disable-next-line no-unused-vars
  return res.status(501).json({ error: 'Not implemented' });
};

const getDecks = async (_req, res) => {
  // eslint-disable-next-line no-unused-vars
  return res.status(501).json({ error: 'Not implemented' });
};

const getDeck = async (_req, res) => {
  // eslint-disable-next-line no-unused-vars
  return res.status(501).json({ error: 'Not implemented' });
};

const updateDeck = async (_req, res) => {
  // eslint-disable-next-line no-unused-vars
  return res.status(501).json({ error: 'Not implemented' });
};

const deleteDeck = async (_req, res) => {
  // eslint-disable-next-line no-unused-vars
  return res.status(501).json({ error: 'Not implemented' });
};

export default { createDeck, getDecks, getDeck, updateDeck, deleteDeck };
