// eslint-disable-next-line no-unused-vars
import Share from '../models/Share.js';

const shareDeck = async (_req, res) => {
  // eslint-disable-next-line no-unused-vars
  return res.status(501).json({ error: 'Not implemented' });
};

export default { shareDeck };
